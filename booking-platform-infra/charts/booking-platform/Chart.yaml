apiVersion: v2
name: booking-platform
description: >-
  Шесть микросервисов booking-platform в Kubernetes: Deployment, Service,
  HPA, KEDA ScaledObject на лаг Kafka и правила трафика Istio
  (DestinationRule с outlier detection + VirtualService с retries).
type: application
version: 1.0.0
appVersion: "1.0.0"
