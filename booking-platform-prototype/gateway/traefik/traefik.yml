api:
  dashboard: true
  insecure: true   # только для локального прототипа; в проде — за аутентификацией

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    exposedByDefault: false
  file:
    filename: /etc/traefik/dynamic.yml
    watch: true

log:
  level: INFO

accessLog: {}
